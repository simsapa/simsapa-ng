# Tasks — StarDict Dictionary Import

Source PRD: [prd-stardict-dictionary-import.md](./prd-stardict-dictionary-import.md)

## Relevant Files

- `backend/migrations/dictionaries/<new-timestamp>_add_user_dict_columns/up.sql` — New Diesel migration adding `is_user_imported BOOLEAN NOT NULL DEFAULT 0`, `language TEXT NULL`, `indexed_at TIMESTAMP NULL`, `description TEXT NULL` to the `dictionaries` table.
- `backend/migrations/dictionaries/<new-timestamp>_add_user_dict_columns/down.sql` — Reverse migration.
- `backend/src/db/dictionaries_schema.rs` — Update Diesel schema to include the three new columns.
- `backend/src/db/dictionaries_models.rs` — Update `Dictionary` and `NewDictionary` structs; add helpers.
- `backend/src/db/dictionaries.rs` — Add: `list_user_dictionaries()` (SELECT must include `description`), `count_words_for_dictionary()`, `rename_dictionary_label()`, `set_indexed_at(id, ts)`, `list_dictionaries_needing_index()`, `is_label_taken_by_shipped()` (implemented via `SELECT DISTINCT dict_words.source_uid` joined to `dictionaries.is_user_imported = 0`, NOT via `dictionaries.label`), `list_shipped_source_uids()`.
- `backend/src/stardict_parse.rs` — Refactor `import_stardict_as_new()` to accept `is_user_imported`, `language`, `description`, optional progress callback; SQL-only — no FTS5 / Tantivy writes. Also extend the StarDict `.ifo` parser to extract the optional `description=` field and surface it to callers.
- `backend/src/search/indexer.rs` — Add `index_dict_words_into_dict_index()`, `delete_from_dict_index_by_source_uid()`, `list_orphan_source_uids_in_dict_index()`. Use `source_uid` term for filtering (per `build_dict_schema`).
- `backend/src/search/fts5_dict.rs` (or wherever the existing dictionary FTS5 helpers live) — Add `insert_dict_words_into_fts5()`, `delete_from_fts5_by_source_uid()`, `list_orphan_source_uids_in_fts5()`.
- `backend/src/dict_index_reconcile.rs` — **NEW** Startup reconciliation pass: drops orphan FTS5 + Tantivy entries, indexes any `dictionaries` row with `indexed_at IS NULL`, exposes a progress callback for the startup window.
- `backend/src/app_data.rs` — Add `export_user_dictionaries(import_dir)` (writes `import-me/user_dictionaries.sqlite3`) and `import_user_dictionaries(import_dir)` (reads it back, re-keys ids, sets `indexed_at = NULL`), following the existing `export_user_chanting_data` pattern. Wire them into `export_user_data_to_assets()` and the matching importer entry point. Confirm/ensure the dictionaries-DB replace step is gated on a successful export.
- `backend/src/dictionary_manager_core.rs` — **NEW** Backend orchestration layer used by the bridge: `import_user_zip()`, `delete_user_dictionary()`, `rename_user_dictionary()`, `validate_label()`. Holds the single `Mutex<()>` that serialises import / rename / delete.
- `backend/src/search/searcher.rs` and `backend/src/query_task.rs` — Accept a `UserDictFilter` and add a `source_uid IN (...)` constraint to the dictionary query path.
- `backend/src/app_settings.rs` — Add per-user-dict enabled-state map (`dict_search.user_dict_enabled.<label>`), plus built-in toggles `dict_search.dpd_enabled` (default true) and `dict_search.commentary_definitions_enabled` (preserve the existing key used by the old "Commentary Definitions in Search" advanced-options checkbox; default true).
- `bridges/src/dictionary_manager.rs` — **NEW** Rust bridge exposing import/list/rename/delete + per-dict enabled-state + progress signals.
- `bridges/src/sutta_bridge.rs` — Extend the dictionary search call to accept the user-dict filter (or add a sibling method). Update qmllint stub in `assets/qml/com/profoundlabs/simsapa/SuttaBridge.qml`.
- `bridges/src/lib.rs` — Register the new bridge module.
- `bridges/build.rs` — Register the new Rust file under `rust_files` and add new QML files under `qml_files`.
- `assets/qml/com/profoundlabs/simsapa/DictionaryManager.qml` — **NEW** qmllint type stub.
- `assets/qml/com/profoundlabs/simsapa/qmldir` — Declare the new bridge type.
- `assets/qml/DictionariesWindow.qml` — **NEW** ApplicationWindow modelled on `SuttaLanguagesWindow.qml`.
- `assets/qml/DictionaryImportDialog.qml` — **NEW** dialog asking for label + language; validates label and warns on unknown tokenizer language.
- `assets/qml/DictionaryEditDialog.qml` — **NEW** edit-label dialog with re-index-on-restart warning.
- `assets/qml/DictionaryListItem.qml` — **NEW** row component for the imported-dictionary list.
- `assets/qml/SearchBarInput.qml` — Modify `advanced_options_row` in place: wrap existing children under a chevron-collapsible "Filters" sub-section; add a chevron-collapsible "Dictionaries" sub-section that mounts the new panel below. **Remove** the existing "Commentary Definitions in Search" checkbox + info button — its UI moves into the new Dictionaries panel as a built-in row (the underlying setting key is preserved).
- `assets/qml/DictionarySearchDictionariesPanel.qml` — **NEW** collapsible "Dictionaries" content panel mounted inside `SearchBarInput.qml`'s `advanced_options_row`. Lists two fixed built-in rows ("DPD", "Commentary Definitions") above the user-imported rows; each row has checkbox + info button + lock toggle.
- `assets/qml/DictionaryInfoDialog.qml` — **NEW** small `Dialog` that displays a description string passed in as a property; used by the info button on every Dictionaries-panel row.
- `assets/qml/DictionaryIndexProgressWindow.qml` — **NEW** modal startup window shown while the reconciliation pass runs.
- `assets/qml/SuttaSearchWindow.qml` — Add `Windows > Dictionaries…` menu item.
- `assets/qml/main.qml` (or whichever launches `SuttaSearchWindow`) — Show `DictionaryIndexProgressWindow.qml` first when the backend reports work to do.
- `backend/src/theme_colors_light.json`, `backend/src/theme_colors_dark.json` — Add a light-blue background colour key for active dictionary rows.
- `backend/src/theme_colors.rs` — Expose the new colour key to QML through the existing channel.
- `backend/src/db/dictionaries.rs` (tests) — Rust unit tests for new DB helpers.
- `backend/src/dict_index_reconcile.rs` (tests) — Tests for orphan-detection + idempotent re-indexing.
- `backend/src/stardict_parse.rs` (tests) — Test the SQL-only import path.
- `backend/tests/dictionary_manager_integration.rs` — Integration test for the bridge round-trip (import → list → rename → delete) followed by simulated startup reconciliation.

### Notes

- All filesystem existence checks must use `try_exists()` per CLAUDE.md (Android safety).
- Any new QML files must be added to `bridges/build.rs` `qml_files` list.
- New Rust bridge modules must be added to `bridges/build.rs` `rust_files` and have a `qmllint` stub + `qmldir` entry.
- Per project memory, run `make build -B` (not direct cmake) after each top-level task; don't run `make qml-test` unless explicitly asked; only run tests after the full top-level task is done.
- Per project memory, do not use bulk `sed` to rename symbols — use targeted Edit calls.
- After each top-level task the project must compile cleanly with `make build -B`.

## Tasks

> Architecture note: All FTS5 / Tantivy index writes happen exclusively in the **startup reconciliation pass** (task 3.0), shown via a modal startup progress window before `SuttaSearchWindow` opens. Inside the running app, import / rename / delete only touch SQL, then ask the user to restart. This avoids contention with the live searcher and dodges Tantivy `IndexWriter` directory-lock issues. The release-upgrade story uses the existing `app_data.rs::export_user_data_to_assets()` → `import-me/user_dictionaries.sqlite3` → re-import pattern (task 5.0), mirroring how chanting / books / bookmarks are round-tripped. The source `.zip` is not archived; SQL is the source of truth.

- [x] 1.0 Database & migration — add `is_user_imported`, `language`, `indexed_at` columns and supporting CRUD
  - [x] 1.1 Create new migration directory under `backend/migrations/dictionaries/2026-04-28-120000_add_user_dict_columns/` with `up.sql` adding three columns (`is_user_imported BOOLEAN NOT NULL DEFAULT 0`, `language TEXT NULL`, `indexed_at TIMESTAMP NULL`) and `down.sql` dropping them. **Note:** the existing `dictionaries` table already has a `description VARCHAR` column from the initial migration, so it is not added here; the existing column is reused for the StarDict `.ifo` description.
  - [x] 1.2 Update `backend/src/db/dictionaries_schema.rs` to add `is_user_imported -> Bool`, `language -> Nullable<Text>`, `indexed_at -> Nullable<Timestamp>` to the `dictionaries` table macro. (`description -> Nullable<Text>` already present.)
  - [x] 1.3 Update `Dictionary` struct in `backend/src/db/dictionaries_models.rs` (and `NewDictionary`) to include the three new fields (`is_user_imported`, `language`, `indexed_at`); `description` already present.
  - [x] 1.4 Update existing `create_dictionary` callers (`import_stardict_as_new` in `stardict_parse.rs`) so they keep compiling with the new fields. The `find_or_create_dpd_dictionary` caller uses `..Default::default()` so the derived `Default` covers the new fields. No `cli/src/bootstrap/` callers exist.
  - [x] 1.5 Add `pub fn list_user_dictionaries(&self) -> Result<Vec<Dictionary>>` filtering `is_user_imported = true`, ordered by `label`.
  - [x] 1.6 Add `pub fn count_words_for_dictionary(&self, dictionary_id: i32) -> Result<i64>` and `pub fn list_dictionaries_needing_index(&self) -> Result<Vec<Dictionary>>` (returns user-imported rows where `indexed_at IS NULL`).
  - [x] 1.7 Add `pub fn rename_dictionary_label(&self, old_label: &str, new_label: &str) -> Result<()>` that, in a single transaction: updates `dictionaries.label`, updates `dict_words.dict_label`, rewrites `dict_words.uid` from `<word>/<old_label>` to `<word>/<new_label>`, and sets `dictionaries.indexed_at = NULL`. Implementation note: the schema column is `dict_words.dict_label` (not `source_uid`); the rename rewrites both the `dictionaries.label` and `dict_words.dict_label` columns plus the `<word>/<label>` suffix on `dict_words.uid`.
  - [x] 1.8 Add `pub fn set_indexed_at(&self, dictionary_id: i32, ts: NaiveDateTime) -> Result<()>`, `pub fn list_shipped_source_uids(&self) -> Result<HashSet<String>>` (executes `SELECT DISTINCT dict_words.dict_label FROM dict_words INNER JOIN dictionaries ON dict_words.dictionary_id = dictionaries.id WHERE dictionaries.is_user_imported = 0`), and `pub fn is_label_taken_by_shipped(&self, label: &str) -> Result<bool>`.
  - [x] 1.9 Migrations auto-apply at runtime via the existing Diesel runner in `backend/src/db/mod.rs` (`run_pending_migrations(DICTIONARIES_MIGRATIONS)` at line ~268). The new migration directory is picked up by `embed_migrations!`.
  - [x] 1.10 `make build -B` succeeds (full link OK; only stale warnings reported).

- [x] 2.0 SQL-only import / rename / delete pipeline (no in-app indexing) + serialisation mutex
  - [x] 2.1 Added `StardictImportProgress` enum (`Extracting`, `Parsing`, `InsertingWords { done, total }`, `Done`, `Failed { msg }`) in `backend/src/stardict_parse.rs`.
  - [x] 2.2 Refactored `import_stardict_as_new(unzipped_dir, lang, label, _ignore_synonyms, delete_if_exists, limit, is_user_imported, description, progress)` returning `Result<i32, String>`. Emits progress at chunk boundaries. Updated cli caller. Added `read_ifo_description()` helper (the `.ifo` description is already parsed by the upstream `stardict` crate, so a thin wrapper suffices). On insert failure the parent `dictionaries` row is rolled back.
  - [x] 2.3 Created `backend/src/dictionary_manager_core.rs` with `static DICT_MGR_LOCK: Mutex<()>`. Each public mutating fn `try_lock`s and returns `BUSY_MSG` on contention. Registered in `backend/src/lib.rs`.
  - [x] 2.4 `import_user_zip(zip_path, label, lang, on_progress)`: validates label format, rejects shipped/user collisions, extracts the zip into `tempfile::Builder::new().prefix("simsapa-stardict-").tempdir_in(simsapa_dir)`, scans for any `*.ifo` file (root or one level deep) and uses its file-stem as the **physical_stem** while keeping the user-chosen **label** as the logical label, captures `.ifo` description, calls `import_stardict_as_new(.., physical_stem, label, is_user_imported=true, ..)`, drops temp dir. The `import_stardict_as_new` signature was updated to take `physical_stem` and `new_dict_label` separately so the user may freely rename a `.zip`'s internal stem to any allowed label.
  - [x] 2.5 `delete_user_dictionary(id)`: looks the row up via `list_user_dictionaries()` (so non-user rows are implicitly rejected), then `delete_dictionary_by_label`.
  - [x] 2.6 `rename_user_dictionary(id, new_label)`: validates label, rejects shipped + user collisions (excluding the row itself), delegates to `db::dictionaries::rename_dictionary_label` which rewrites `dict_words.uid` / `dict_words.dict_label` and sets `indexed_at = NULL` in a transaction.
  - [x] 2.7 `validate_label`: ASCII alnum + `_-`, non-empty.
  - [x] 2.7a `suggested_label_for_zip(zip_path)`: replaces non-`[A-Za-z0-9_-]` chars with `_`, collapses runs of `_`, trims `_-` from edges, returns `""` if empty after sanitisation. Tests will cover the doc-string examples (`dppn.zip`, `My Dict v2.zip`, `日本語.zip`) in 9.1.
  - [x] 2.8 `make build -B` succeeds (link OK).

- [x] 3.0 Startup reconciliation pass: orphan cleanup + indexing + progress window
  - [x] 3.1 Added `index_dict_words_into_dict_index(index_dir, lang, words, on_progress)` in `backend/src/search/indexer.rs`. Mirrors the bootstrap StarDict field set (uid/uid_rev/word/synonyms/language/source_uid/content/content_exact/is_bold_definition=false), uses `register_tokenizers(index, lang)` (falls back via `snowball::lang_to_algorithm` which returns English for unknown codes — same fallback as the bootstrap path; no hard error on unknown lang), emits progress every 1000 words.
  - [x] 3.2 Added `delete_from_dict_index_by_source_uid(index_dir, label)` that scans every per-language subdir of the unified dict index and deletes by `Term::from_field_text(source_uid_field, label)`, commits + waits for merging threads.
  - [x] 3.3 Added `list_indexed_source_uids_in_dict_index(index_dir) -> Result<HashSet<String>>` using the inverted-index term-dictionary stream over the `source_uid` field — efficient for large indexes (no full doc scan).
  - [x] 3.4 Skipped a separate FTS5 helper module: `dict_words_fts` is auto-synced via the SQLite triggers defined in `scripts/dictionaries-fts5-indexes.sql` (insert/update/delete on `dict_words` propagate to `dict_words_fts`). Orphans cannot accumulate in FTS5 from in-app deletes for that reason; release-upgrade swap of `dictionaries.sqlite3` brings a fresh FTS5 table from the new shipped DB. The reconcile pass therefore only manages Tantivy. Documented in `dict_index_reconcile.rs` module docstring.
  - [x] 3.5 Created `backend/src/dict_index_reconcile.rs` with `reconcile_dict_indexes(on_progress)`:
        1. Compute orphan set as `tantivy_source_uids - {label of every current `dictionaries` row}` and drop each via `delete_from_dict_index_by_source_uid`.
        2. For each user-imported dictionary with `indexed_at IS NULL`: pre-clear by source_uid (idempotency), load its `dict_words`, call `index_dict_words_into_dict_index`, then `set_indexed_at(Utc::now().naive_utc())`.
  - [x] 3.6 Added `ReconcileProgress` enum with `DroppingOrphans { done, total }`, `IndexingDictionary { label, done, total, dict_index, dict_total }`, `Done`.
  - [x] 3.7 Wired into startup: added `reconcile_dict_indexes_needed_c()` and `reconcile_dict_indexes_blocking_c()` FFI in `backend/src/lib.rs`. `cpp/gui.cpp` calls them between `init_app_data()`/`check_and_configure_for_first_start()` and `create_sutta_search_window()`. After reconciliation, `reinit_fulltext_searcher()` is called so the searcher picks up the new segments. NOTE: Tantivy stale-lockfile detection deferred — single-user desktop+Android contention is not expected because reconciliation runs before the searcher opens; if encountered later, the existing `IndexWriter::open` error path can be augmented.
  - [x] 3.8 Created `assets/qml/DictionaryIndexProgressWindow.qml` — modal `ApplicationWindow` with stage `Label` + `ProgressBar`. Bridge signal wiring (reconcile_progress / reconcile_finished) is left for task 4.0 when the `DictionaryManager` bridge is created; the file currently exposes the properties (`stage_text`, `progress_value`, `indeterminate`) the bridge will drive.
  - [x] 3.9 Pre-bridge integration: `cpp/gui.cpp` blocks on `reconcile_dict_indexes_blocking_c()` before `create_sutta_search_window()`. Once task 4.0 lands the bridge with worker-thread progress signals, the gating logic can be flipped to `if (reconcile_needed) show progress window; bridge.start_reconcile(); on reconcile_finished -> create_sutta_search_window()`.
  - [x] 3.9a Bridge-driven progress window wired up: `DictionaryIndexProgressWindow.qml` instantiates `DictionaryManager`, calls `start_reconcile()` on `Component.onCompleted`, drives the progress bar from `reconcileProgress`, and closes itself on `reconcileFinished`. `cpp/gui.cpp` loads the QML via a local `QQmlApplicationEngine` and pumps a `QEventLoop` until the window's QQuickWindow becomes invisible / is destroyed, then proceeds to create `SuttaSearchWindow`. The synchronous `reconcile_dict_indexes_blocking_c()` is kept as a fallback for the unlikely QML-load-failure case.
  - [x] 3.10 `make build -B` succeeded.

- [x] 4.0 `DictionaryManager` Rust bridge + QML registration + persisted enabled-state in `app_settings`
  - [x] 4.1 Created `bridges/src/dictionary_manager.rs` modelled on `bridges/src/storage_manager.rs` / `asset_manager.rs`, exposing a `DictionaryManager` QObject with CXX-Qt and `cxx_qt::Threading`.
  - [x] 4.2 Bridge methods exposed: `import_zip(zip_path, label, lang) -> QString` (worker thread; emits progress + finished signals); `list_user_dictionaries() -> QString` (JSON `[{id, label, title, language, entry_count, description}]`); `rename_label(id, new_label)`; `delete_dictionary(id)`; `label_status(label)` returning `"available" | "taken_user" | "taken_shipped" | "invalid"` via `db::dictionaries::is_label_taken_by_shipped` (DISTINCT `dict_words.dict_label` for non-user dicts); `suggested_label_for_zip(zip_path)` (sanitised filename stem); `is_known_tokenizer_lang(lang)` against the `snowball::lang_to_algorithm` known-list. All mutating ops route through `dictionary_manager_core`.
  - [x] 4.3 Bridge signals declared: `importProgress(stage, done, total)`, `importFinished(dictionary_id, label)`, `importFailed(message)`, `reconcileProgress(stage, done, total)`, `reconcileFinished()`. Worker threads emit via `qt_thread.queue(...)` so signals dispatch on the Qt main thread.
  - [x] 4.4 Added persisted state in `backend/src/app_settings.rs`: new fields `dict_search_dpd_enabled` (default true) and `dict_search_user_dict_enabled: IndexMap<String, bool>`; reused existing `include_comm_bold_definitions_in_search_results` for the Commentary Definitions toggle (default flipped to `true` to match new spec; serde-default annotated so existing JSON values are honoured). `backend/src/app_data.rs` gained `get_dpd_enabled` / `set_dpd_enabled` / `get_commentary_definitions_enabled` / `set_commentary_definitions_enabled` / `get_user_dict_enabled` / `set_user_dict_enabled` / `list_user_dict_enabled` plus a private `persist_app_settings` helper.
  - [x] 4.5 Bridge methods for the enabled-state map (`get_user_dict_enabled` / `set_user_dict_enabled` / `get_user_dict_enabled_map` returning a JSON object, plus DPD + Commentary Definitions getters/setters).
  - [x] 4.6 Bridge entry-points `reconcile_needed()` and `start_reconcile()` (spawns worker thread, emits `reconcileProgress` / `reconcileFinished`).
  - [x] 4.7 Created QML stub `assets/qml/com/profoundlabs/simsapa/DictionaryManager.qml` declaring every bridge function and signal with placeholder return values.
  - [x] 4.8 Added `DictionaryManager 1.0 DictionaryManager.qml` to `assets/qml/com/profoundlabs/simsapa/qmldir`.
  - [x] 4.9 Registered `src/dictionary_manager.rs` in `bridges/build.rs` `rust_files`. (Other new QML files — DictionariesWindow, dialogs, panel, info dialog — will be added under their owning tasks 6.0 / 7.0; `DictionaryIndexProgressWindow.qml` was already registered in task 3.8.)
  - [x] 4.10 Registered the bridge in `bridges/src/lib.rs`.
  - [x] 4.11 `make build -B` succeeded.

- [x] 5.0 Release-upgrade round-trip: `export_user_dictionaries` + `import_user_dictionaries` in `app_data.rs` (temp-sqlite, mirrors chanting)
  > Investigation findings: (a) The existing flow in `bridges/src/sutta_bridge.rs::prepare_for_database_upgrade` runs `export_user_data_to_assets()` first, and **only** writes the `delete_files_for_upgrade.txt` marker if that succeeds. The marker is what triggers `check_delete_files_for_upgrade()` in `lib.rs:717` to delete `dictionaries.sqlite3` (and the others) on the next startup. So the gate already exists — adding our `export_user_dictionaries` to the same export aggregator means the dictionary DB cannot be replaced unless our snapshot has been written. No new gate required.
  > (b) The pre-existing `import-me/` clear at `app_data.rs:~1555` was a blanket `remove_dir_all`. That would have wiped a stale `user_dictionaries.sqlite3` snapshot. Replaced with a selective clear that excludes `user_dictionaries.sqlite3` (task 5.6).
  - [x] 5.1 Added `pub fn export_user_dictionaries(&self, import_dir: &Path) -> Result<()>` in `backend/src/app_data.rs`. Loads user-imported `dictionaries` rows + their `dict_words` rows, creates `import-me/user_dictionaries.sqlite3` via Diesel `establish` + `run_dictionaries_migrations` (made `pub` in `db/mod.rs`), and inserts rows in a single transaction. `indexed_at` is forced to `NULL`; `description` is carried over. All path checks use `try_exists()`.
  - [x] 5.2 Wired into `export_user_data_to_assets()` alongside `export_user_books` / `export_user_bookmarks` / `export_user_chanting_data` as a `user_dictionaries` per-category error. Confirmed gating: the upgrade flow already requires successful export before the dictionaries DB is deleted (see investigation note above).
  - [x] 5.3 Added `pub fn import_user_dictionaries(&self, import_dir: &Path) -> Result<()>`. Reads `user_dictionaries.sqlite3`, inserts each `dictionaries` row via autoincrement (skipping if a row with the same label already exists in the live DB — the existing id is reused for re-keying), records `old_id -> new_id` in a HashMap, then inserts `dict_words` rows with the rewritten `dictionary_id`. Whole thing runs in a single transaction; on failure the snapshot stays. On success the snapshot file is removed (`std::fs::remove_file`).
  - [x] 5.4 Wired `import_user_dictionaries` into `import_user_data_from_assets` alongside the existing book/bookmark/chanting importers. Also taught the trailing `remove_dir_all(&import_dir)` cleanup to preserve `user_dictionaries.sqlite3` if it survived (the import-failed retry case).
  - [x] 5.5 Snapshot reader uses Diesel's typed schema (`Dictionary::as_select()` / `DictWord::as_select()`) which selects explicit columns rather than `SELECT *`. Inserts use explicit per-column `.values(( ... ))` tuples. Documented schema-drift contract in the doc-comment.
  - [x] 5.6 `export_user_dictionaries` returns `Err` if `import-me/user_dictionaries.sqlite3` already exists ("A previous dictionary upgrade hasn't completed; please restart the app to finish it before upgrading again."). The blanket `remove_dir_all` of `import-me/` at the top of `export_user_data_to_assets` and at the tail of `import_user_data_from_assets` was replaced with selective clears that preserve the snapshot file.
  - [x] 5.7 FK audit: `dict_words` has only one FK to `dictionaries.id` — `dictionary_id`. No other FK columns require re-keying. Confirmed via `db/dictionaries_schema.rs`.
  - [x] 5.8 Snapshot consumption uses `std::fs::remove_file` (no copy is performed; rows are read from the snapshot DB directly via Diesel and inserted into the live DB, so no temp duplicate exists on disk).
  - [x] 5.9 `make build -B` succeeded.

- [ ] 6.0 `DictionariesWindow.qml` + import / edit / delete dialogs + `Windows > Dictionaries…` menu
  - [x] 6.1 In `assets/qml/SuttaSearchWindow.qml`, locate the `Windows` menu and add a `MenuItem` "Dictionaries…" that opens `DictionariesWindow.qml` (use the existing Loader / lazy pattern of the language window menu). Implementation: added `Dictionaries…` `CMenuItem` calling `SuttaBridge.open_dictionaries_window()`. New plumbing: `cpp/dictionaries_window.{h,cpp}`, `WindowManager::create_dictionaries_window()` (reuses an existing window if open), `callback_open_dictionaries_window()` in `gui.cpp`, exposed to Rust via `api.rs` and `SuttaBridge::open_dictionaries_window`. Stub added in `assets/qml/com/profoundlabs/simsapa/SuttaBridge.qml`.
  - [x] 6.2 Create `assets/qml/DictionariesWindow.qml` as an `ApplicationWindow`, modelled on `SuttaLanguagesWindow.qml`. Layout: header with "Import StarDict…" button; below it a `ListView` of imported dictionaries; below that an inline progress area for active import.
  - [x] 6.3 Bind the `ListView` model to `DictionaryManager.list_user_dictionaries()` results (parse JSON in QML on `Component.onCompleted` and after `import_finished` / rename / delete). Empty-state label: "No imported dictionaries yet."
  - [x] 6.4 Create `assets/qml/DictionaryListItem.qml` showing: title, label, language, entry count, an Edit button, and a trash icon. Wire Edit to `DictionaryEditDialog.qml`; wire trash to a `MessageDialog` confirm → `DictionaryManager.delete_dictionary(id)` → show the close-and-restart `MessageDialog` → refresh list.
  - [x] 6.5 Create `assets/qml/DictionaryImportDialog.qml`: `FileDialog` filtered to `*.zip`. On accept, call `DictionaryManager.suggested_label_for_zip(filePath)` and open a `Dialog` with the returned string pre-filled into the `label` `TextField` (the user may freely edit it before submitting); also a `lang` `TextField` (default `pli`). Live-validate label via a single `DictionaryManager.label_status(label)` call: `"invalid"` → inline error; `"taken_shipped"` → inline error "This name is reserved by a built-in dictionary."; `"taken_user"` → on submit show `MessageDialog` offering **Replace** (calls `delete_dictionary` of the existing then proceeds) or **Cancel**; `"available"` → submit allowed.
        > ⚠ **Replace-flow contract:** `dictionary_manager_core::import_user_zip` rejects user-label collisions with an explicit "already exists" error — the *caller* is responsible for handling Replace. The Replace branch must therefore: (a) look up the existing user dictionary's `id` from `list_user_dictionaries()`, (b) call `DictionaryManager.delete_dictionary(id)` and wait for it to return success (it is synchronous; busy responses must abort with an error), and (c) only then call `import_zip(...)`. Do not call `import_zip` first expecting a `force_replace` flag — there isn't one. Surfacing the busy state during Replace is also important, since delete + import are two separate mutex-guarded operations.
        > ⚠ **Stem-vs-label decoupling (impl note):** `import_user_zip` now scans the extracted archive for any `*.ifo` and uses its file-stem internally as the physical stem; the user-chosen label only needs to satisfy the label-format / collision rules. No filename matching is required between the `.zip` name and the label.
  - [x] 6.6 Lang-warning UX: in the import dialog, after the user enters a `lang` not recognised by `DictionaryManager.is_known_tokenizer_lang(lang)`, show an inline warning: "Unknown tokenizer language. Indexing will use the default tokenizer." Non-blocking — the user may proceed.
  - [x] 6.7 Render the inline progress area (Rectangle + `ProgressBar` + stage label) hooked to `import_progress` / `import_finished` / `import_failed`. On `import_finished`, show a brief success indication ("Import completed") and then the close-and-restart `MessageDialog`. On `import_failed`, show the error message and clear the progress area.
  - [x] 6.8 Create `assets/qml/DictionaryEditDialog.qml`: a `TextField` for the new label, with visible warning text "Renaming takes effect after the next app restart, when the affected entries are re-indexed in FTS5 and Tantivy. This may take some time for large dictionaries." Confirm calls `DictionaryManager.rename_label(id, new_label)` and shows the close-and-restart `MessageDialog`.
  - [x] 6.9 Disable Import / Edit / Delete buttons whenever a `Busy` response is returned (i.e. another op is in progress); re-enable on `import_finished` / error. Implementation note: the per-row Edit/Delete buttons + the top-bar Import button are all gated on `root.busy`, which the window sets when it kicks off `import_zip()` and clears on `import_finished` / `import_failed`. Mid-flight `Busy` responses from the bridge are surfaced through the same error_dialog path and clear `root.busy`.
  - [x] 6.10 Verify `make build -B` succeeds.

- [x] 7.0 Advanced search options: wrap existing inputs as collapsible "Filters" sub-section in `SearchBarInput.qml`, add new "Dictionaries" sub-section (with built-in DPD + Commentary Definitions rows on top of user-imported rows)
  > Scope clarification: the advanced options row already exists in `assets/qml/SearchBarInput.qml` inside `Flow { id: advanced_options_row }`, gated by the `advanced_options_btn` toggle. This task modifies that block in place — `DictionaryTab.qml` is **not** the right file. The new "Dictionaries" UI is implemented as a separate QML component but mounted inside `advanced_options_row`.
  - [x] 7.1 `advanced_options_row` was converted from `Flow` to `ColumnLayout`. Existing children wrapped under a chevron-collapsible "Filters" header (chevron + bold "Filters" Label), with `is_filters_collapsed: false` controlling visibility of an inner `Flow { id: filters_flow }` containing all original RowLayouts. The "Commentary Definitions in Search" CheckBox+Info Button pair was removed (the underlying `set_/get_include_comm_bold_definitions_in_search_results` SuttaBridge keys remain intact and are still consumed by `SuttaSearchWindow.qml`).
  - [x] 7.2 Added a chevron-collapsible "Dictionaries" header inside the same `advanced_options_row` (visible only when `search_area === "Dictionary"`), with `is_dictionaries_collapsed: false`. The new `DictionarySearchDictionariesPanel { ... }` is mounted directly below it.
  - [x] 7.3 Created `assets/qml/DictionarySearchDictionariesPanel.qml` with `user_dicts`, `dpd_enabled`, `commentary_definitions_enabled`, `locked_label` properties and a `selection_changed()` signal.
  - [x] 7.4 Panel renders DPD row, Commentary Definitions row, then a `Repeater` over `user_dicts`. Each row is a `Rectangle` wrapper containing a `CheckBox`, title (and `(label)` for user-imported), info `Button`, and a lock `Button` (push-pin icon).
  - [x] 7.4a Created `assets/qml/DictionaryInfoDialog.qml` (read-only `TextArea` in a `ScrollView`). DPD row uses a stock description, Commentary Definitions row reuses the explanatory text from the removed Commentary Definitions checkbox, and user-imported rows show their `description` (info button hidden when `description` is null/empty).
  - [x] 7.5 Active-checkbox row background uses a light-blue tint derived from `palette.highlight` (alpha 0.18). The existing `theme_colors.rs` is hardcoded to Qt palette roles, so adding a new key would require expanding the JSON struct + every entry; instead a transparent-tinted highlight color is computed inline at the QML layer, which adapts to light/dark themes automatically.
  - [x] 7.6 DPD/Commentary/user-dict checkboxes are wired to `DictionaryManager.set_dpd_enabled` / `set_commentary_definitions_enabled` / `set_user_dict_enabled`; initial state pulled from the matching getters via `refresh_state()`. Every toggle emits `selection_changed()`.
  - [x] 7.7 Lock toggle behaviour: each row has an effective identifier (`__dpd__`, `__commentary_definitions__`, or the user-dict label). `toggle_lock()` deactivates if same id, else switches `locked_label` to the new id. Persisted checkbox values are never modified by lock.
  - [x] 7.8 When `locked_label !== ""`, every other row sets `enabled: false` on its CheckBox + lock Button (not on the info Button so it remains tappable for reference) and dims `opacity` to 0.5.
  - [x] 7.9 Empty-state hint Label ("No imported dictionaries. See Windows > Dictionaries…") appears below the two always-visible built-in rows when `user_dicts.length === 0`.
  - [x] 7.10 Panel refreshes via `Component.onCompleted` and a `Connections { target: DictionaryManager; function onImportFinished(...) { refresh_state(); } }`. Re-mounting on `search_area === "Dictionary"` toggling visible re-binds the panel's existing state. `DictionariesWindow` close-triggered refresh is deferred (the panel re-reads from the bridge whenever the dictionary area is reopened with a new query).
  - [x] 7.11 `DictionarySearchDictionariesPanel.selection_changed` is wired to `advanced_options_debounce_timer.restart()` directly inside `SearchBarInput.qml`.
  - [x] 7.12 `make build -B` succeeded.

- [x] 8.0 Wire per-dict enabled set + built-in DPD/Commentary toggles + transient lock state into the dictionary search query layer
  - [x] 8.1 Added `dict_source_uids: Option<Vec<String>>` to `SearchParams` (`backend/src/types.rs`) and `SearchFilters` (`backend/src/search/types.rs`), plus a matching field on `SearchQueryTask`. The single `Option<Vec<String>>` collapses what the original PRD called `Solo` and `IncludeOnly` into one shape: both reduce to "restrict dict_words to this set"; the QML caller picks which set to pass. `None` = no constraint, `Some([])` = no dict_words rows match, `Some([labels…])` = those labels only. Bold-definition rows are gated independently by the existing `include_comm_bold_definitions` flag.
  - [x] 8.2 Extended `add_dict_filters` in `backend/src/search/searcher.rs` to OR the bold gate with the `dict_source_uids` set. The construction is `(is_bold = false AND source_uid IN set) OR (is_bold = true AND include_bold_definitions)`; when neither branch is satisfiable (`Some([])` + `include_bold = false`), an explicit contradiction (`is_bold MUST true AND MUST_NOT true`) forces zero results. Default behaviour (`dict_source_uids = None`) preserves the legacy MustNot-bold rule.
  - [x] 8.3 SQL dict paths apply the inclusion set as a uniform post-filter in `results_page` for `search_area = Dictionary` and any non-Fulltext mode (`apply_dict_source_uids_filter`). Bold-definition rows (`table_name != "dict_words"`) are never dropped here. `total` is decremented by the number of dropped rows so pagination buttons stay coherent. Tantivy already pushes the constraint down — the post-filter only runs on Contains / Headword / DpdLookup / UidMatch.
  - [x] 8.4 `SuttaSearchWindow.qml::get_search_params_from_ui()` now branches when `search_area === "Dictionary"` and calls a new `compute_dict_search_filter()` helper. It reads the panel's `locked_label`, `user_dicts`, `dpd_enabled`, `commentary_definitions_enabled`, and combines them with the new `DictionaryManager` bridge helpers (`dpd_source_uids`, `commentary_definitions_source_uids`, `list_shipped_source_uids`). Lock branches: `__dpd__` → only DPD uids + bold off; `__commentary_definitions__` → empty dict set + bold on; user-label → just that label + bold off. Unlocked: union of (other-shipped ∪ DPD-if-enabled ∪ each enabled user-label) + bold gate from panel state. An empty union returns zero results (no fallback), per PRD.
  - [x] 8.4a Added `dpd_source_uids()` (returns `["dpd"]`), `commentary_definitions_source_uids()` (queries `db::dpd::list_distinct_bold_def_ref_codes`), and `list_shipped_source_uids()` (delegates to `db::dictionaries::list_shipped_source_uids`) on `bridges/src/dictionary_manager.rs`. Stub functions added to the qmllint `DictionaryManager.qml`.
  - [x] 8.5 Skipped a second sibling dictionary search method on `SuttaBridge`; the existing `results_page(query, page_num, search_area, params_json)` already accepts a JSON-blob params map, so the new `dict_source_uids` field rides the existing channel through `serde_json` deserialisation into `SearchParams`. The `SuttaBridge.qml` qmllint stub did not need a new function.
  - [x] 8.6 Sanity: with no user dictionaries and both DPD + Commentary on, the filter built by `compute_dict_search_filter` is `IncludeOnly(shipped_source_uids ∪ {"dpd"})` + `include_comm_bold_definitions = true`. Since `list_shipped_source_uids` already contains `"dpd"` (DPD's `dict_label`), this is equivalent to the pre-feature behaviour; the unified Tantivy index is restricted only to source_uids that already have rows. Note: the SQL post-filter drops rows whose source_uid is not in the set, which is also a no-op in this case because every dict_words row's `dict_label` is in `shipped_source_uids`.
  - [x] 8.7 `make build -B` succeeded.

- [ ] 9.0 Tests + final `make build -B` + PROJECT_MAP.md / docs update
  - [ ] 9.1 Rust unit tests in `backend/src/db/dictionaries.rs` for `list_user_dictionaries`, `count_words_for_dictionary`, `rename_dictionary_label`, `set_indexed_at`, `is_label_taken_by_shipped` (verify a bold-definitions `ref_code` value returns true even though no `dictionaries.label` matches it — this is the whole point of computing the set from `dict_words.source_uid`), `list_shipped_source_uids`, `list_dictionaries_needing_index`. Test `suggested_label_for_zip` for: simple stem (`dppn.zip`), spaces (`My Dict v2.zip`), all-non-ASCII (`日本語.zip` → empty), runs collapsed (`a   b.zip` → `a_b`). Use the local appdata DB; no `#[ignore]`.
  - [ ] 9.2 Test `dictionary_manager_core::import_user_zip` against a small fixture `.zip` (commit a tiny one under `backend/tests/fixtures/`) — include one variant whose `.ifo` has a `description=` line and one without. Verify SQL row exists + `indexed_at IS NULL` + `description` matches the `.ifo` value (or NULL when absent) + temp extraction dir is cleaned up.
  - [ ] 9.3 Test `dictionary_manager_core::delete_user_dictionary` removes SQL row (cascade `dict_words`) and refuses on shipped rows.
  - [ ] 9.4 Test `dictionary_manager_core::rename_user_dictionary` rewrites uids + sets `indexed_at = NULL` + refuses on shipped rows + refuses on built-in label collision.
  - [ ] 9.5 Test `dict_index_reconcile::reconcile_dict_indexes` end-to-end against the local appdata DB: precondition rows with `indexed_at IS NULL` are indexed; orphan FTS5 / Tantivy entries (synthesised) are removed; reruns are no-ops; interrupted runs (kill mid-loop) re-converge on next call.
  - [ ] 9.6 Test `dictionary_manager_core` mutex serialisation (concurrent calls return `Busy` for all but one).
  - [ ] 9.7 Test `app_data::export_user_dictionaries` + `import_user_dictionaries` round-trip: import → export to temp dir → wipe live DB rows → import from temp dir → assert row + `dict_words` count match (with re-keyed ids and `indexed_at = NULL`).
  - [ ] 9.8 Run `cd backend && cargo test`; pre-existing failures are ignored per memory but flag any newly introduced ones.
  - [ ] 9.9 Run `make build -B` one final time; resolve any compilation issues.
  - [ ] 9.10 Update `PROJECT_MAP.md`: new bridge, new QML files (window + dialogs + panels + info dialog + index progress window), `is_user_imported` / `language` / `indexed_at` / `description` columns + migration, new `dict_index_reconcile.rs` and `dictionary_manager_core.rs` modules, `export_user_dictionaries` / `import_user_dictionaries` round-trip via `import-me/user_dictionaries.sqlite3`, relocation of the "Commentary Definitions in Search" toggle from advanced options into the new Dictionaries panel, and the new "DPD in Search" toggle.
  - [ ] 9.11 Update `docs/` with a brief user-facing note covering: how to import a StarDict zip (the dialog pre-fills the label from the filename — feel free to edit), label rules (built-in labels reserved — including bold-definitions ref_codes), the close-and-restart flow, how the Dictionaries panel works (built-in DPD + Commentary Definitions rows live alongside imported dictionaries; checkboxes toggle inclusion; the lock button solos a single dictionary across the entire built-in + user set), info buttons (built-ins always have one; user dictionaries show one when the StarDict `.ifo` provided a description), what to do if the startup re-indexing window appears slow (it is one-shot per change), and what to do if a release upgrade is interrupted ("restart the app to finish processing the previous upgrade before starting a new one").
